---
kind: pipeline
name: default

clone:

steps:
  - name: deploy-test
    image: joomlaprojects/docker-images:php7.3
    environment:
      JHOST:
        from_secret: jissues_host
      JUSERNAME:
        from_secret: jissues_username
      JKEY:
        from_secret: jissues_key
    commands:
      - TEMP=$(mktemp)
      - echo "$JKEY" > $TEMP
      - ssh -o 'StrictHostKeyChecking no' -i $TEMP $JUSERNAME@$JHOST 'bash -s' < "echo 1"

---
kind: signature
hmac: 636745455223159c1565bc6e572a2e6cd625d75c783f783ce68222a81f468353

...
