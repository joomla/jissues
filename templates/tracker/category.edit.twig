{# Copyright (C) 2012 - 2014 Open Source Matters, Inc. All rights reserved. #}
{# GNU General Public License version 2 or later; see LICENSE.txt #}

{% extends "index.twig" %}

{% block title %}{{ parent() }} | Edit Category{% endblock %}

{% block prependMenu %}
    <li>
        <a class="nav-link bg-success" href="javascript:;" onclick="document.editForm.submit();">Save</a>
    </li>

    {{ parent() }}

{% endblock %}

{% block content %}
    <div class="page-header">
        <h1>Edit Category</h1>
    </div>

    <form method="post" action="{{ uri.base.path }}category/save" class="form-inline" name="editForm">
        <fieldset>
            <legend>Details</legend>

            <div class="mb-3">
                <label class="form-label" for="title">Title</label>
                <input class="form-control" type="text" name="category[title]" id="title" value="{{ item.title }}"/>
            </div>

            <div class="mb-3">
                <label class="form-label" for="alias">Alias</label>
                <input class="form-control" type="text" name="category[alias]" id="alias" value="{{ item.alias }}"/>
            </div>

            <div class="mb-3">
                <label class="form-label" for="color_display">Colour</label>
                <input class="color_select" id="color" value="#{{ item.color }}"/>
                <input class="form-control" type="text" name="category[color]" id="color_display" value="{{ item.color }}"/>
            </div>

            <input type="hidden" name="category[project_id]" value="{{ project.project_id }}"/>
            <input type="hidden" name="category[id]" id="id" value="{{ item.id }}" />

        </fieldset>
    </form>

{% endblock %}

{% block JavaScriptBottom %}
    {{ parent() }}

    <script src="{{ asset('js/color-select.js', 'debug') }}"></script>
{% endblock %}
